package com.training.runner;

import org.junit.runner.RunWith;

import io.cucumber.junit.Cucumber;
import io.cucumber.junit.CucumberOptions;

@RunWith(Cucumber.class)
@CucumberOptions(features="/Users/rahade/workspaceMobileAutomation/CucumberFramework2/features/Login/Login.feature",
glue={"com.training.steps"},
dryRun = true
//"dry run" is a way to check the feature files and step definitions for syntax errors or any missing step definitions without actually executing the test steps. It allows you to verify that everything is set up correctly before running the tests fully.
/*
 * 
 * Dry run is used to check each step in feature file matches with the step definition.
 * 
 * If every implementation is correct then you will see a message good to go.
 * 
 * No execution code in the step definition file.
 * 
 */
	
		)

public class TestRunner {

}
